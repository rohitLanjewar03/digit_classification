# digit_classification
Digit Classification Neural Network built from scratch using NumPy only. Implements forward &amp; backward propagation, ReLU and Softmax activations, cross-entropy loss, accuracy evaluation, and result visualization without using any ML libraries.
